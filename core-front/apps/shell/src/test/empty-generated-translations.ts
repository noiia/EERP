// Vitest stand-in for src/generated/generated-translations.ts (a gitignored build
// artefact that may not exist when tests run). Registers nothing: tests seed the
// translationRegistry themselves so they control exactly which catalogs exist.
export {}
